% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_cc.R
\name{calc_cc}
\alias{calc_cc}
\title{Estimate cloud cover from shortwave radiation}
\usage{
calc_cc(
  datetime,
  airt,
  swr,
  relh = NULL,
  dewt = NULL,
  lat,
  lon,
  elev = 0,
  tz = "Etc/GMT-12"
)
}
\arguments{
\item{datetime}{\code{Date} or POSIXct timestamps matching \code{swr}.}

\item{airt}{air temperature, degC.}

\item{swr}{measured downwelling shortwave radiation, W m-2.}

\item{relh}{relative humidity, percent; used when \code{dewt} is absent.}

\item{dewt}{dew point temperature, degC.}

\item{lat, lon}{decimal degrees.}

\item{elev}{elevation, m above sea level.}

\item{tz}{timezone that \code{datetime} refers to; used to expand daily dates
into hours. Default \code{"Etc/GMT-12"} (fixed NZST).}
}
\value{
numeric vector of cloud cover fractions (0-1), same length as
\code{datetime}. Values that cannot be inferred (measured shortwave at or
above clear sky, or night) are linearly interpolated from neighbours.
}
\description{
Inverts the ratio of measured to clear-sky potential shortwave radiation
to a cloud fraction, following Martin & McCutcheon (1999). Works at any
timestep: daily input is evaluated on an internal hourly grid (built in
\code{tz}) and aggregated back to daily.
}
\examples{
d <- data.frame(Date = seq(as.Date("2024-01-01"), by = "day", length.out = 10),
                airt = 18, swr = c(300, 120, 280, 90, 310, 150, 200, 260, 80, 290))
calc_cc(d$Date, airt = d$airt, swr = d$swr, relh = 70,
        lat = -38.08, lon = 176.27, elev = 280)
}
