% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_era5_grib_point.R
\name{read_era5_grib_point}
\alias{read_era5_grib_point}
\title{Read a point from an ERA5 GRIB file}
\usage{
read_era5_grib_point(file, shape = NULL, lat, lon, method = "bilinear")
}
\arguments{
\item{file}{character; path to the GRIB file. Can be a vector of paths.}

\item{shape}{sf object; polygon over which to take a weighted mean. If
\code{NULL} (default) the value is extracted at \code{lon}/\code{lat}.}

\item{lat}{numeric; latitude.}

\item{lon}{numeric; longitude.}

\item{method}{character; interpolation method passed to
\code{\link[terra:extract]{terra::extract()}} for point extraction. Default "bilinear".}
}
\value{
A data frame with columns \code{DateTime}, \code{value}, \code{units}, \code{variable}
and \code{short_name}, sorted by \code{DateTime}.
}
\description{
Extract a time series at a point (or the weighted mean over a polygon) from
one or more GRIB files downloaded with \code{\link[=download_era5_cds]{download_era5_cds()}}.
}
\details{
Requires the suggested package \pkg{terra} (and \pkg{sf} when \code{shape} is
supplied).
}
\examples{
\dontrun{
lat <- -38.07782
lon <- 176.2673
files <- download_era5_cds(lat = lat, lon = lon, year = 2024, month = 1:2,
                           variable = "2m_temperature", path = "data/test",
                           user = "person@email.com")
df <- read_era5_grib_point(file = files, lat = lat, lon = lon)
}
}
\seealso{
\code{\link[=download_era5_cds]{download_era5_cds()}}
}
