% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_obs_met.R
\name{prepare_obs_met}
\alias{prepare_obs_met}
\title{Standardise a table of locally measured meteorology for bias correction}
\usage{
prepare_obs_met(
  obs,
  col_map = NULL,
  datetime_col = NULL,
  date_format = NULL,
  tz = "Etc/GMT-12",
  resample = c("none", "hour", "day"),
  interval = c("ending", "beginning"),
  derive = TRUE,
  convert_units = TRUE,
  station = NA_character_,
  verbose = TRUE
)
}
\arguments{
\item{obs}{data frame, or path to a \code{.csv} / \code{.rds} file.}

\item{col_map}{optional named character vector mapping AEME names to the
columns in \code{obs}, e.g.
\code{c(MET_tmpair = "AirTemp_C", MET_wndspd = "WindSpd", MET_radswd = "SolarRad")}.
Columns not listed are dropped. If \code{NULL}, columns already named
\verb{MET_*} are kept and the rest are matched with \code{\link[=guess_met_vars]{guess_met_vars()}}.}

\item{datetime_col}{name of the timestamp column; auto-detected when
\code{NULL} (first column named or containing \code{date}/\code{time}).}

\item{date_format}{optional \code{strptime} format string for the timestamp
column. When \code{NULL} (default) a set of common formats is tried; if the
strings contain a time (a \code{:}), only date-time formats are considered
so that a stray time-less row does not silently collapse the whole
column to dates.}

\item{tz}{time zone of the observation timestamps. Default
\code{"Etc/GMT-12"} = fixed NZST (matches the extractor default). Use
\code{"Pacific/Auckland"} if the logger recorded civil time with DST.}

\item{resample}{\code{"none"} (keep native step, de-duplicated), \code{"hour"}
(mean per hour; rain/snow summed) or \code{"day"} (daily mean; rain/snow
summed; \code{Date} returned as class \code{Date}).}

\item{interval}{which hour a sub-hourly record belongs to when
\code{resample = "hour"}. \code{"ending"} (default) puts a record at 00:15 into
the hour labelled 01:00, matching the accumulated-flux convention of
ERA5 and \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}, so the two line up when joined;
\code{"beginning"} labels it 00:00. With \code{"ending"} the first bin of a
record that starts exactly on the hour holds a single observation.
Ignored for \code{resample = "day"}, which always uses calendar days.}

\item{derive}{if \code{TRUE} (default) fill \code{MET_wndspd}/\code{MET_wnddir} from
u/v (or vice versa) and \code{MET_tmpdew}<->\code{MET_humrel} from air
temperature, where the inputs are present.}

\item{convert_units}{if \code{TRUE} (default) auto-detect and fix common unit
mistakes (K->degC, hPa->Pa, RH 0-1 -> \%, km/h->m/s, mm->... ).}

\item{station}{optional label stored on the result (\code{"buoy"},
\code{"shore"}, station id) - carried into the bias-correction metadata.}

\item{verbose}{print what was matched / converted / resampled.}
}
\value{
data frame, \code{Date} first, then the matched \verb{MET_*} columns.
Attributes: \code{tz}, \code{resolution} (\code{"subhourly"}/\code{"hourly"}/\code{"daily"}),
\code{station}, \code{n_obs}.
}
\description{
Takes a user-supplied observation table (lake buoy, shore weather
station, council / NIWA / CliFlo export, logger dump) and returns a
clean data frame with a \code{Date} column and AEME \verb{MET_*} columns in AEME
units, resampled to the resolution you intend to bias-correct at. The
output is designed to line up with \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}} output so
the two can be joined on \code{Date} in \code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}}.
}
\details{
Standard names / units (see \code{\link[=met_vars]{met_vars()}}):
\code{MET_tmpair} degC, \code{MET_tmpdew} degC, \code{MET_humrel} \%, \code{MET_wndspd} m/s,
\code{MET_wnddir} deg (FROM), \code{MET_wnduvu}/\code{MET_wnduvv} m/s, \code{MET_radswd} and
\code{MET_radlwd} W/m2, \code{MET_pprain}/\code{MET_ppsnow} mm (per timestep),
\code{MET_prsttn}/\code{MET_prmslp} Pa.
}
\seealso{
\code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}}, \code{\link[=apply_met_bias_correction]{apply_met_bias_correction()}}
}
