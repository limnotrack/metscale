% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardise_met.R
\name{standardise_met}
\alias{standardise_met}
\title{Standardise meteorological names and units}
\usage{
standardise_met(met, verbose = TRUE)
}
\arguments{
\item{met}{data frame with a date/time column and meteorological columns.}

\item{verbose}{report each rename and conversion.}
}
\value{
\code{met} with standard names and units, the time column renamed to
\code{Date}. Unmatched columns are kept unchanged with a warning.
}
\description{
Renames columns to the standard \verb{MET_*} names with \code{\link[=guess_met_vars]{guess_met_vars()}},
then detects and corrects common unit mistakes from the value ranges:
Kelvin to degC, hPa to Pa, humidity fraction to percent, oktas to
fraction, km/h to m/s, metres to millimetres, and MJ or kJ m-2 day-1 to
W m-2.
}
\examples{
raw <- data.frame(timestamp = as.Date("2024-01-01") + 0:4,
                  AirTemp_K = c(291, 289, 292, 288, 293),
                  WindSpd_kmh = c(11, 18, 7, 22, 11),
                  SolarRad = c(300, 120, 280, 90, 310),
                  Rain_mm = c(0, 12, 0, 25, 0))
standardise_met(raw)
}
