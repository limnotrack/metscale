% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_met.R
\name{expand_met}
\alias{expand_met}
\title{Expand a minimal meteorological set to a complete one}
\usage{
expand_met(met, lat, lon, elev = 0, tz = "Etc/GMT-12", round_to = 3)
}
\arguments{
\item{met}{data frame with a \code{Date} column (\code{Date} or POSIXct) and the
\verb{MET_*} columns above. Any timestep; nothing is resampled.}

\item{lat, lon}{lake position, decimal degrees.}

\item{elev}{lake surface elevation, m above sea level.}

\item{tz}{timezone the timestamps refer to, used for the solar geometry
in \code{\link[=calc_cc]{calc_cc()}}. Default \code{"Etc/GMT-12"} (fixed NZST). Ignored when
\code{MET_cldcvr} is supplied.}

\item{round_to}{decimal places for the returned values, or \code{NULL} to
leave unrounded.}
}
\value{
data frame with \code{Date} and \code{MET_radswd}, \code{MET_radlwd},
\code{MET_cldcvr}, \code{MET_tmpair}, \code{MET_humrel}, \code{MET_tmpdew}, \code{MET_prvapr},
\code{MET_prsttn}, \code{MET_prmslp}, \code{MET_wndspd}, \code{MET_wnddir}, \code{MET_wnduvu},
\code{MET_wnduvv}, \code{MET_pprain}, \code{MET_ppsnow}.
}
\description{
Derives every variable a lake model may ask for from a small required
core, filling in only what is missing: relative humidity and dew point
from each other, vapour pressure, cloud cover from measured shortwave
(\code{\link[=calc_cc]{calc_cc()}}), downwelling longwave (\code{\link[=calc_in_lwr]{calc_in_lwr()}}), station and sea
level pressure, wind speed/direction and u/v components, and snowfall.
}
\details{
Required: \code{MET_radswd} (W m-2), \code{MET_tmpair} (degC), \code{MET_pprain} (mm per
timestep), one of \code{MET_humrel} (percent) / \code{MET_tmpdew} (degC), and wind
as either \code{MET_wndspd} (m/s) or both \code{MET_wnduvu} and \code{MET_wnduvv}.
}
\examples{
met <- data.frame(Date = seq(as.Date("2024-01-01"), by = "day", length.out = 5),
                  MET_radswd = c(300, 120, 280, 90, 310),
                  MET_tmpair = c(18, 16, 19, 15, 20),
                  MET_humrel = c(70, 85, 65, 90, 60),
                  MET_wndspd = c(3, 5, 2, 6, 3),
                  MET_pprain = c(0, 12, 0, 25, 0))
expand_met(met, lat = -38.08, lon = 176.27, elev = 280)
}
