% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disaggregate_met.R
\name{build_diurnal_climatology}
\alias{build_diurnal_climatology}
\title{Mean diurnal cycle of an hourly meteorological record}
\usage{
build_diurnal_climatology(hourly, tz = NULL, vars = NULL, n_sub = 24)
}
\arguments{
\item{hourly}{data frame with \code{Date} (POSIXct) and \verb{MET_*} columns, e.g.
from \code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}}.}

\item{tz}{timezone in which the diurnal cycle is expressed. Defaults to
the \code{tz} attribute of \code{hourly}, else \code{"Etc/GMT-12"}.}

\item{vars}{variables to summarise; default all \verb{MET_*} columns present.}

\item{n_sub}{steps per day (24 hourly, 8 three-hourly).}
}
\value{
object of class \code{diurnal_climatology}: a list with \code{shape} (a
named list of \verb{12 x n_sub} matrices), \code{kind} (\code{"additive"} or
\code{"ratio"} per variable), \code{n_days} per month, \code{tz} and \code{n_sub}.
}
\description{
Summarises an hourly record into, for each variable and calendar month,
the average shape of the day: an anomaly about the daily mean for
additive variables (temperature, humidity, pressure, longwave) and a
normalised factor for multiplicative ones (wind speed, shortwave,
precipitation). Used by \code{\link[=disaggregate_met_to_hourly]{disaggregate_met_to_hourly()}} with
\code{method = "diurnal"}.
}
\examples{
set.seed(1)
h <- data.frame(
  Date = seq(as.POSIXct("2024-01-01", tz = "Etc/GMT-12"),
             by = "hour", length.out = 24 * 60),
  MET_tmpair = 15 + 5 * sin(seq_len(24 * 60) * 2 * pi / 24) + rnorm(24 * 60))
dc <- build_diurnal_climatology(h)
round(dc$shape$MET_tmpair[1, ], 2)
}
