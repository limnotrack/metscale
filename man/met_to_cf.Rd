% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_to_cf.R
\name{met_to_cf}
\alias{met_to_cf}
\title{Convert AEME \verb{MET_*} meteorology to CF / CMIP variable names and units}
\usage{
met_to_cf(
  met,
  vars = NULL,
  timestep = NULL,
  time_name = "time",
  outfile = NULL
)
}
\arguments{
\item{met}{data frame with a date/time column plus \verb{MET_*} columns, or a
path to a \code{.csv} / \code{.rds} file holding one.}

\item{vars}{optional subset of \verb{MET_*} columns to convert; default every
mapped column present.}

\item{timestep}{seconds represented by one row, used only for the
precipitation/snowfall flux conversion. \code{NULL} (default) infers it from
the median spacing of the time column; pass it explicitly for an
irregular series or a single row.}

\item{time_name}{name for the time column in the output. Default
\code{"time"} (CF); pass \code{"Date"} to keep the AEME name.}

\item{outfile}{optional path; when given the result is written with
\code{utils::write.csv(row.names = FALSE)} and returned invisibly.}
}
\value{
the data frame with CF short-name columns in CF units.
Attributes \code{cf_units} and \code{cf_standard_names} (named by the output
columns) and \code{timestep_s} record the conversion.
}
\description{
Renames the AEME \verb{MET_*} columns (\code{\link[=met_vars]{met_vars()}}) to the short variable
names used by CMIP6 / CORDEX and the CF conventions, and converts each to
its CF-canonical unit. \code{cf_to_met()} is the inverse.
}
\details{
\tabular{llll}{
   AEME \tab CF / CMIP \tab CF standard_name \tab unit \cr
   \code{MET_tmpair} \tab \code{tas} \tab air_temperature \tab K \cr
   \code{MET_tmpdew} \tab \code{tdps} \tab dew_point_temperature \tab K \cr
   \code{MET_humrel} \tab \code{hurs} \tab relative_humidity \tab \% \cr
   \code{MET_prsttn} \tab \code{ps} \tab surface_air_pressure \tab Pa \cr
   \code{MET_prmslp} \tab \code{psl} \tab air_pressure_at_mean_sea_level \tab Pa \cr
   \code{MET_prvapr} \tab \code{pvap} \tab water_vapor_partial_pressure_in_air \tab Pa \cr
   \code{MET_wndspd} \tab \code{sfcWind} \tab wind_speed \tab m s-1 \cr
   \code{MET_wnddir} \tab \code{sfcWindDir} \tab wind_from_direction \tab degree \cr
   \code{MET_wnduvu} \tab \code{uas} \tab eastward_wind \tab m s-1 \cr
   \code{MET_wnduvv} \tab \code{vas} \tab northward_wind \tab m s-1 \cr
   \code{MET_radswd} \tab \code{rsds} \tab surface_downwelling_shortwave_flux_in_air \tab W m-2 \cr
   \code{MET_radlwd} \tab \code{rlds} \tab surface_downwelling_longwave_flux_in_air \tab W m-2 \cr
   \code{MET_cldcvr} \tab \code{clt} \tab cloud_area_fraction \tab \% \cr
   \code{MET_pprain} \tab \code{pr} \tab precipitation_flux \tab kg m-2 s-1 \cr
   \code{MET_ppsnow} \tab \code{prsn} \tab snowfall_flux \tab kg m-2 s-1 \cr
}


Unit changes: air and dew-point temperature degC -> K; vapour pressure
hPa -> Pa; cloud cover fraction -> percent; precipitation and snowfall
"mm per timestep" -> a \verb{kg m-2 s-1} flux (dividing by the timestep in
seconds - 1 mm of water over 1 m2 is 1 kg). Wind, radiation and the
surface/sea-level pressures are already in CF units and pass through
unchanged. Columns that are not \verb{MET_*} (an id column, say) are carried
through untouched.
}
\examples{
met <- data.frame(
  Date = seq(as.POSIXct("2024-01-01", tz = "UTC"), by = "hour", length.out = 5),
  MET_tmpair = c(15, 15.4, 16, 16.2, 15.8),
  MET_wndspd = c(3, 4, 3.5, 5, 4.2),
  MET_pprain = c(0, 0.2, 1.1, 0, 0.4))
met_to_cf(met)
}
\seealso{
\code{\link[=cf_to_met]{cf_to_met()}} for the inverse, \code{\link[=met_vars]{met_vars()}}, \code{\link[=standardise_met]{standardise_met()}}
and \code{\link[=guess_met_vars]{guess_met_vars()}} for bringing arbitrary names \emph{to} \verb{MET_*}.
}
