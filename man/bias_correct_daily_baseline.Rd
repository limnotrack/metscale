% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_met.R
\name{bias_correct_daily_baseline}
\alias{bias_correct_daily_baseline}
\title{Build a bias-corrected daily baseline for a climate-scenario workflow}
\usage{
bias_correct_daily_baseline(
  era5,
  bc = NULL,
  lat = NULL,
  lon = NULL,
  elev = 0,
  tz = NULL,
  expand = TRUE,
  min_frac = 0.5,
  verbose = TRUE
)
}
\arguments{
\item{era5}{hourly reanalysis met, from \code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}}.}

\item{bc}{a \code{met_biascorr} from \code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}}; \code{NULL} to
aggregate without correcting.}

\item{lat, lon, elev}{lake position; taken from the attributes of \code{era5}
when \code{NULL}.}

\item{tz}{timezone for day boundaries and solar geometry; defaults to
the \code{tz} attribute of \code{era5}.}

\item{expand}{regenerate the dependent variables with \code{\link[=expand_met]{expand_met()}}
(default \code{TRUE}).}

\item{min_frac}{minimum fraction of a day that must be present for that
day to be kept, passed to \code{\link[=met_to_daily]{met_to_daily()}}.}

\item{verbose}{print progress.}
}
\value{
daily data frame with \code{Date} of class \code{Date} and \verb{MET_*}
columns, carrying a \code{biascorr} attribute describing the correction.
}
\description{
Applies a fitted \code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}} to a full hourly reanalysis
record, aggregates to daily and regenerates the dependent variables, so
the result is ready to be handed to a delta-change routine such as
\code{AEME}'s \code{climate_shift()} as the baseline meteorology.
}
\details{
The rainfall column is always populated from the corrected record, so
that downstream code which special-cases an all-zero \code{MET_pprain} (as
\code{climate_shift()} does, by substituting inflow-derived rainfall) does
not take that branch unintentionally.
}
\examples{
\dontrun{
era5 <- extract_era5_lake_met("LID 11133", path = nc_dir, lakes = lakes,
                              method = "area", years = 1980:2024)
obs  <- prepare_obs_met("obs/rotorua_buoy_met.csv", resample = "hour")
bc   <- fit_met_bias_correction(era5, obs)

baseline <- bias_correct_daily_baseline(era5, bc, elev = 279)

## hand to AEME's delta-change routine
inp <- AEME::input(aeme); inp$meteo <- baseline
AEME::input(aeme) <- inp
}
}
\seealso{
\link{scenario_workflow} for the full delta-change pipeline and
\code{vignette("scenario-workflow", package = "climprep")}.
}
