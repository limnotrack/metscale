% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_climate_point.R
\name{climate_point_monthly_climatology}
\alias{climate_point_monthly_climatology}
\title{Equal-weighted monthly climatology from CMIP6 / CCAM point files}
\usage{
climate_point_monthly_climatology(
  path,
  lon,
  lat,
  vars = c("MET_tmpair", "MET_pprain", "MET_wndspd", "MET_radswd", "MET_humrel",
    "MET_radlwd", "MET_prsttn"),
  experiments = NULL,
  method = c("bilinear", "nearest"),
  calendar = c("auto", "365_day", "360_day", "366_day", "standard"),
  years = NULL,
  fun = mean,
  verbose = TRUE
)
}
\arguments{
\item{path}{directory holding the \code{.nc} files, or a character vector of
file paths.}

\item{lon, lat}{point of interest, decimal degrees (WGS84).}

\item{vars}{variables to return, as AEME \verb{MET_*} names or CMIP short names
(do not mix the two). Default is the seven mapped variables; only those
present on disk are returned.}

\item{experiments}{optional character vector to keep (e.g.
\code{c("historical", "ssp245")}); \code{NULL} (default) returns every experiment
found.}

\item{method}{\code{"bilinear"} (default) interpolation between the four
surrounding grid nodes, or \code{"nearest"} grid node.}

\item{calendar}{\code{"auto"} (default, read from the \code{time} variable's
\code{calendar} attribute), or force one of \code{"365_day"}, \code{"360_day"},
\code{"366_day"}, \code{"standard"}. A \verb{365_day} / \verb{360_day} series is mapped onto
real dates, so it has no 29 February (and, for \verb{360_day}, no 31st).}

\item{years}{optional integer vector of calendar years (as decoded onto
real dates) to include, e.g. a historical or future reference window.
\code{NULL} (default) uses every year found.}

\item{fun}{the averaging function applied per experiment/variable/month.
Default \code{mean}.}

\item{verbose}{print each file as it is read.}
}
\value{
a data frame with \code{experiment}, \code{variable} (AEME \verb{MET_*} name),
\code{month} (integer \code{1:12}) and \code{value}.
}
\description{
Companion to \code{\link[=extract_climate_point]{extract_climate_point()}}, for building the monthly
delta-change factors of \link{scenario_workflow}. \code{\link[=extract_climate_point]{extract_climate_point()}}
merges variables onto a shared \code{Date} and, to do that safely, averages
together any model-days a fixed-length calendar's month-end clamp maps
onto the same real date (see its documentation) - which very slightly
down-weights those clamped days in a monthly mean taken from its output,
since they then count as a single row instead of several. This function
instead reads each variable's own per-file series independently and
averages straight by calendar month, before any Date-based merge or
clamp-collapse happens, so every model day counts equally regardless of
how many (or how few) real dates it lands on.
}
\examples{
\dontrun{
clim <- climate_point_monthly_climatology("inst/extdata/rotorua_cmip6",
                                        lon = 176.2717, lat = -38.0790,
                                        years = 2005:2014)
}
}
\seealso{
\code{\link[=extract_climate_point]{extract_climate_point()}}, \link{scenario_workflow}
}
