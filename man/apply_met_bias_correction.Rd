% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_met_bias_correction.R
\name{apply_met_bias_correction}
\alias{apply_met_bias_correction}
\title{Apply a fitted ERA5 met bias correction to a full record}
\usage{
apply_met_bias_correction(
  era5,
  bc,
  vars = NULL,
  clamp = TRUE,
  swr_ceiling = 1361,
  expand = FALSE,
  lat = NULL,
  lon = NULL,
  elev = NULL,
  tz = NULL,
  verbose = TRUE
)
}
\arguments{
\item{era5}{data frame with \code{Date} + \verb{MET_*} columns.}

\item{bc}{a \code{met_biascorr} object from \code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}}.}

\item{vars}{subset of \code{names(bc$models)} to apply; default all.}

\item{clamp}{clamp to physical bounds after correcting (default \code{TRUE}):
\code{MET_pprain}/\code{MET_ppsnow}/\code{MET_radswd}/\code{MET_radlwd}/\code{MET_wndspd} >= 0,
\code{MET_humrel} within 0 to 100, \code{MET_radswd} <= \code{swr_ceiling}.}

\item{swr_ceiling}{hard upper bound for corrected \code{MET_radswd} (W/m2).
Default 1361 (solar constant); pass a clear-sky series (same length as
\code{era5}) for a tighter, time-varying cap.}

\item{expand}{if \code{TRUE}, keep \code{Date} + corrected primaries and call
\code{\link[=expand_met]{expand_met()}} to rebuild the full set. Needs \code{lat}/\code{lon}/\code{elev}
(taken from \code{attr(era5, "lat")} / \code{"lon"} when \code{NULL}).}

\item{lat, lon, elev}{lake position for \code{expand}.}

\item{tz}{timezone for the solar geometry in \code{\link[=expand_met]{expand_met()}}; defaults to
the \code{tz} attribute of \code{era5}, else \code{"Etc/GMT-12"}.}

\item{verbose}{report which variables were corrected.}
}
\value{
\code{era5} with corrected columns (same class/attributes), plus
\code{attr(., "biascorr")} recording the training window and methods. When
\code{expand = TRUE}, the \code{\link[=expand_met]{expand_met()}} output frame.
}
\description{
Takes the object from \code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}} and applies it to any
ERA5 met frame from \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}} /
\code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}} - typically the full record, not just the
training window. Each corrected variable is clamped back to physical
bounds. Optionally the corrected primaries are expanded so the
dependent variables (dew point, vapour pressure, cloud cover, longwave,
u/v wind, station pressure) are regenerated consistently with
\code{\link[=expand_met]{expand_met()}}.
}
\seealso{
\code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}}, \code{\link[=met_to_daily]{met_to_daily()}}, \code{\link[=expand_met]{expand_met()}}
}
