% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_era5.R
\name{plot_era5}
\alias{plot_era5}
\title{Quick-look plot of an ERA5 / ERA5-Land GRIB or netCDF file}
\usage{
plot_era5(
  file,
  var = NULL,
  time = NULL,
  fun = c("mean", "sum", "min", "max", "sd", "median"),
  point = NULL,
  engine = c("base", "ggplot2"),
  col = "viridis",
  max_vars = 6L,
  verbose = TRUE
)
}
\arguments{
\item{file}{path to an ERA5 / ERA5-Land file (netCDF or GRIB). A vector of
paths is allowed and is stacked in time, per variable.}

\item{var}{optional character vector selecting which variable(s) to draw,
matched case-insensitively against the ERA5 short name / GRIB element
(e.g. \code{"t2m"}, \code{"2T"}, \code{"tp"}). \code{NULL} (default) draws every variable in
the file, capped at \code{max_vars}.}

\item{time}{which slice to map. \code{NULL} (default) aggregates every step
with \code{fun}; otherwise a single step given as a \code{POSIXct} / \code{Date}, a
\code{"YYYY-MM-DD HH:MM"} string (nearest step is used), or an integer layer
index (negative counts from the end).}

\item{fun}{aggregation for the map when \code{time = NULL}: one of \code{"mean"}
(default), \code{"sum"}, \code{"min"}, \code{"max"}, \code{"sd"}, \code{"median"}.}

\item{point}{optional \code{c(lon, lat)} in decimal degrees (WGS84). When
given, the time-series panel is a bilinear sample at that point and the
point is marked on the map; otherwise the series is the grid mean.}

\item{engine}{\code{"base"} (default) draws with \code{\link[terra:plot]{terra::plot()}} and base
graphics and returns the summary invisibly; \code{"ggplot2"} returns a
faceted \code{ggplot} of the map(s) (single variable - it takes the first
when several are present) and falls back to \code{"base"} when \pkg{ggplot2}
is not installed.}

\item{col}{a vector of colours for the map, or the name of an
\code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette. Default \code{"viridis"}.}

\item{max_vars}{maximum number of variables to draw (default \code{6}).}

\item{verbose}{print a one-line summary per variable.}
}
\value{
For \code{engine = "base"}, invisibly a data frame with one row per
drawn variable: \code{variable}, \code{label}, \code{units}, \code{n_layers}, \code{t_start},
\code{t_end}, \code{xmin} / \code{xmax} / \code{ymin} / \code{ymax}, \code{res_x} / \code{res_y} and
\code{min} / \code{mean} / \code{max} of the mapped field. The per-variable time series
are attached as \code{attr(., "series")} (a named list of
\code{data.frame(time, value)}) and the aggregated maps as \code{attr(., "maps")}
(a named list of \code{SpatRaster}). For \code{engine = "ggplot2"}, the \code{ggplot}
object carrying the same attributes.
}
\description{
A fast visual check of a downloaded reanalysis file, drawn straight from
whatever is on disk. For every variable found in the file(s) it draws a
map of the field aggregated over time next to a time series - the spatial
mean over the whole grid, or a bilinear sample at \code{point}. Use it to
eyeball the footprint, the value range and the temporal coverage before
handing the file to \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}} /
\code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}}.
}
\details{
The reader backend is chosen from the file extension, matching the
extractors: \code{.grib} / \code{.grb} / \code{.grib2} and everything else are all read
with \pkg{terra}. GRIB bands are grouped into variables by their
\code{GRIB_ELEMENT} metadata; netCDF files by sub-dataset / variable name.
Note that \pkg{terra} sometimes mislabels ERA5 GRIB units (e.g. reporting
\code{K} values under a \code{C} tag); the numbers are drawn as stored.
}
\examples{
\dontrun{
f <- system.file(
  "extdata/era5/reanalysis-era5-land_2m_temperature_hourly_2024_1_toba.grib",
  package = "metscale")

plot_era5(f)                            # map of the monthly mean + grid-mean series
plot_era5(f, point = c(98.6, 2.6))      # series sampled at a point
plot_era5(f, time = "2024-01-15 12:00") # one hour, mapped

## a directory of monthly files for one variable, stacked
plot_era5(list.files("era5_land", "2m_temperature.*\\\\.grib$", full.names = TRUE))
}
}
\seealso{
\code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}, \code{\link[=plot_extract_grid]{plot_extract_grid()}},
\code{\link[=read_era5_grib_point]{read_era5_grib_point()}}
}
