% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_conversions.R
\name{wind_at_height}
\alias{wind_at_height}
\title{Adjust wind speed from one measurement height to another}
\usage{
wind_at_height(
  u,
  from,
  to = 10,
  z0 = 2e-04,
  method = c("log", "power"),
  exponent = 0.11
)
}
\arguments{
\item{u}{wind speed at height \code{from}, m/s (vector).}

\item{from}{measurement height of \code{u}, m.}

\item{to}{target height, m (default 10).}

\item{z0}{roughness length in m for \code{method = "log"} (default \code{2e-4}), or
the string \code{"charnock"} for a wind-speed-dependent open-water roughness.}

\item{method}{\code{"log"} (default) or \code{"power"}.}

\item{exponent}{power-law exponent for \code{method = "power"} (default 0.11).}
}
\value{
wind speed at height \code{to}, m/s, same length as \code{u}.
}
\description{
Buoy and shore anemometers commonly sit 2--3 m above the surface, whereas
reanalyses (ERA5, ERA5-Land) and the lake-model convention report wind at
\strong{10 m}. This rescales a wind-speed series to a different height,
assuming a neutrally-stratified surface layer.
}
\details{
\describe{
\item{\code{method = "log"}}{(default) the logarithmic wind profile,
\verb{u(z) = (u* / kappa) log(z / z0)}, so
\code{u(to) = u(from) * log(to / z0) / log(from / z0)}. \code{z0} is the
aerodynamic roughness length; the default \code{2e-4} m is a typical
open-water value. \code{z0 = "charnock"} instead solves the
Charnock relation \verb{z0 = a u*^2 / g} (with \code{a = 0.013}) iteratively, so
the adjustment grows with wind speed as a real sea/lake surface
roughens.}
\item{\code{method = "power"}}{the power law \code{u(to) = u(from) (to/from)^p}
with \code{p = exponent} (default \code{0.11}, the open-water value; use about
\code{1/7} over land).}
}
For 2 m -> 10 m over water all three give a factor of roughly 1.15--1.20.
The neutral-stability assumption is good for the moderate-to-strong winds
that matter for lake mixing; it over-corrects a little in very light,
stable conditions.
}
\examples{
wind_at_height(5, from = 2)              # 2 m -> 10 m, ~5.9 m/s
wind_at_height(c(2, 6, 12), from = 3, to = 10)
wind_at_height(10, from = 2, z0 = "charnock")
}
\seealso{
\code{\link[=met_wind_at_height]{met_wind_at_height()}} to apply this to a \verb{MET_*} data frame.
}
