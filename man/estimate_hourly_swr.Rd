% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solar.R
\name{estimate_hourly_swr}
\alias{estimate_hourly_swr}
\title{Estimate sub-daily shortwave radiation from daily means}
\usage{
estimate_hourly_swr(
  met,
  lat,
  lon,
  tz = "Etc/GMT-12",
  timestep = c("hour", "3hour"),
  interval = c("ending", "beginning", "instant"),
  cloud = 0
)
}
\arguments{
\item{met}{data frame with a \code{Date} column (class \code{Date}, or POSIXct at
daily resolution) and \code{MET_radswd} in W m-2.}

\item{lat, lon}{position in decimal degrees.}

\item{tz}{timezone the daily dates refer to, and in which the sub-daily
output is labelled. Default \code{"Etc/GMT-12"} (fixed NZST, no daylight
saving).}

\item{timestep}{\code{"hour"} (default) or \code{"3hour"}.}

\item{interval}{what a timestamp denotes: \code{"ending"} (default) means the
value is the mean over the interval \emph{ending} at that label, which is
the convention of ERA5 accumulated fluxes and hence of
\code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}; \code{"beginning"} is the mean over the
following interval; \code{"instant"} is the instantaneous value at the
label. Averaging over an interval is done on a 10-minute sub-grid, so
sunrise and sunset steps are handled correctly.}

\item{cloud}{optional cloud fraction, scalar or one value per row of
\code{met}, used to damp the clear-sky curve before rescaling (the rescale
makes the daily total insensitive to this, but it changes the shape
slightly).}
}
\value{
data frame with \code{Date} (POSIXct in \code{tz}) and \code{MET_radswd}
(W m-2). Days whose clear-sky mean is zero (polar night) return zeros.
}
\description{
Builds a clear-sky irradiance curve from solar geometry and rescales it
so that each day's mean reproduces the supplied daily mean \code{MET_radswd}.
This preserves the observed daily energy while giving a physically
shaped diurnal cycle with genuine night-time zeros.
}
\details{
Unlike the equivalent routine in \code{AEME}, timestamps are handled
explicitly: daily dates are expanded into sub-daily steps \emph{in \code{tz}}, the
solar geometry is evaluated at the corresponding UTC instants, and the
result is returned labelled in \code{tz}. The shortwave peak therefore lands
at local solar noon rather than being offset by the UTC offset.
}
\examples{
met <- data.frame(Date = seq(as.Date("2024-01-01"), by = "day",
                             length.out = 5),
                  MET_radswd = c(300, 120, 280, 90, 310))
hr <- estimate_hourly_swr(met, lat = -38.08, lon = 176.27)
head(hr)
}
