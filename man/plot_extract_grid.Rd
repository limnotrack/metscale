% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_extract_grid.R
\name{plot_extract_grid}
\alias{plot_extract_grid}
\title{Preview the extraction grid, cells and geometry before extracting}
\usage{
plot_extract_grid(
  path,
  lon = NULL,
  lat = NULL,
  geom = NULL,
  method = c("bilinear", "nearest", "area", "area_mean"),
  variables = "2m_temperature",
  pattern = "{variable}",
  years = NULL,
  months = 1:12,
  area_crs = 2193,
  max_dist_km = 50,
  pad = 3,
  engine = c("ggplot2", "base")
)
}
\arguments{
\item{path}{directory holding the ERA5-Land files (netCDF or GRIB), as
passed to \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}.}

\item{lon, lat}{point of interest, decimal degrees (WGS84). Ignored if
\code{geom} is supplied.}

\item{geom}{an \code{sf}/\code{sfc} point or polygon (any CRS); overrides
\code{lon}/\code{lat}. A polygon is required for \code{method = "area"} /
\code{"area_mean"}.}

\item{method}{spatial sampling to preview: \code{"bilinear"} (default),
\code{"nearest"}, \code{"area"} or \code{"area_mean"}. See \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}.}

\item{variables}{ERA5-Land variable name(s); the first one with files on
disk is used to read the grid. Default \code{"2m_temperature"}.}

\item{pattern}{file-name template, as in \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}.
Default \code{"{variable}"}.}

\item{years, months}{optional filters passed to the file matcher (only
needed when \code{pattern} pins \code{{year}} / \code{{month}}).}

\item{area_crs}{projected CRS for the \code{"area"} intersection, matching the
extractor. Default \code{2193} (NZTM 2000).}

\item{max_dist_km}{distance cap for a \code{"nearest"} sample. Unlike the
extractor this only warns (the plot is still drawn) so you can see how
far off a mis-placed point is. Default \code{50}.}

\item{pad}{number of grid cells of context to draw around the geometry.
Default \code{3}.}

\item{engine}{\code{"ggplot2"} (default, returns a \code{ggplot} you can further
style or hand to \code{mapview}/\code{tmap}) or \code{"base"} (draws with
\code{\link[sf:plot]{sf::plot()}}, returns the grid invisibly). \code{"ggplot2"} falls back to
\code{"base"} if \pkg{ggplot2} is not installed.}
}
\value{
For \code{engine = "ggplot2"}, a \code{ggplot} object; for \code{"base"}, the
grid \code{sf} (invisibly). Either way the return carries attributes
\code{grid} (an \code{sf} of the drawn cells with columns \code{ix}, \code{iy}, \code{weight},
\code{selected}, \code{land}), \code{weights} (the \code{data.frame(ix, iy, w)} selection)
and \code{file} (the file read).
}
\description{
A sanity-check map drawn straight from the files that
\code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}} / \code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}} would read: the
ERA5-Land grid around your location, the cell(s) that the chosen \code{method}
would actually sample (shaded by weight), the land/sea validity mask, and
your point or polygon on top. Use it to confirm the geometry lands where
you expect and overlaps sensible cells \emph{before} running an extraction.
}
\details{
The grid, cell selection and weights come from the same internal machinery
as the extractors, so what you see is what you would get. Only one file is
opened (the first that matches \code{pattern} for the first available
\code{variables} entry) - grids are identical across ERA5-Land variables and
months.

On the map:
\itemize{
\item filled cells are the ones \code{method} samples, coloured by their weight
(\code{"nearest"} -> one cell, weight 1; \code{"bilinear"} -> up to four;
\code{"area"} / \code{"area_mean"} -> every cell the polygon overlaps);
\item grey cells are in view but not sampled;
\item a dashed blue outline marks cells masked out by ERA5-Land (sea, or the
lake pixels themselves) - these are dropped and their weight
redistributed;
\item grey dots are grid nodes (cell centres);
\item the red polygon / red cross is your \code{geom} (or \code{lon}/\code{lat}).
}
}
\examples{
\dontrun{
## point, bilinear
plot_extract_grid("era5_land", lon = 176.2717, lat = -38.0790)

## lake polygon, area-weighted - the case worth eyeballing
poly <- sf::st_read("gis/rotorua.gpkg")
p <- plot_extract_grid("era5_land", geom = poly, method = "area")
p + ggplot2::coord_sf(expand = FALSE)

## interactive check against a basemap, no new dependency in the package
mapview::mapview(attr(p, "grid"), zcol = "weight")
}
}
\seealso{
\code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}, \code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}}
}
