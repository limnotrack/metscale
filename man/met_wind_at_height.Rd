% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_conversions.R
\name{met_wind_at_height}
\alias{met_wind_at_height}
\title{Adjust the wind columns of a met data frame to a new height}
\usage{
met_wind_at_height(
  met,
  from,
  to = 10,
  z0 = 2e-04,
  method = c("log", "power"),
  exponent = 0.11
)
}
\arguments{
\item{met}{data frame with \code{MET_wndspd} and/or \code{MET_wnduvu} +
\code{MET_wnduvv}.}

\item{from}{measurement height of the wind columns, m.}

\item{to, z0, method, exponent}{passed to \code{\link[=wind_at_height]{wind_at_height()}}.}
}
\value{
\code{met} with the wind columns rescaled, carrying
\code{attr(., "wind_height") <- to}.
}
\description{
Applies \code{\link[=wind_at_height]{wind_at_height()}} to \code{MET_wndspd} and, when present, the
components \code{MET_wnduvu} / \code{MET_wnduvv} (scaled by the same factor, so wind
direction is unchanged). Use it to bring a 2 m buoy record onto the 10 m
convention before \code{\link[=fit_met_bias_correction]{fit_met_bias_correction()}}.
}
\examples{
d <- data.frame(Date = Sys.time() + 0:3 * 3600,
                MET_wndspd = c(2, 4, 3, 5))
met_wind_at_height(d, from = 2)
}
\seealso{
\code{\link[=wind_at_height]{wind_at_height()}}, \code{\link[=prepare_obs_met]{prepare_obs_met()}} (which takes a
\code{wind_height} argument).
}
