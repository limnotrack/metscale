% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/disaggregate_met.R
\name{disaggregate_met_to_hourly}
\alias{disaggregate_met_to_hourly}
\title{Disaggregate daily meteorology to hourly or 3-hourly}
\usage{
disaggregate_met_to_hourly(
  daily,
  donor,
  method = c("fragments", "diurnal"),
  timestep = c("hour", "3hour"),
  swr = c("clearsky", "shape"),
  interval = c("ending", "beginning", "instant"),
  lat = NULL,
  lon = NULL,
  elev = 0,
  tz = NULL,
  analogue_window = 15,
  match_on_value = TRUE,
  sample_top_k = 1,
  blend_hours = 2,
  seed = NULL,
  expand = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{daily}{data frame with \code{Date} (class \code{Date}, or daily POSIXct) and
\verb{MET_*} columns. Rainfall and snowfall are treated as daily totals,
every other variable as a daily mean.}

\item{donor}{hourly data frame with \code{Date} (POSIXct) and \verb{MET_*}
columns for the same location, e.g. from \code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}}.
A longer donor record gives a better analogue pool.}

\item{method}{\code{"fragments"} (default) or \code{"diurnal"}.}

\item{timestep}{\code{"hour"} (default) or \code{"3hour"}.}

\item{swr}{\code{"clearsky"} (default) to rebuild shortwave from solar
geometry, or \code{"shape"} to treat it like any other multiplicative
variable. \code{"clearsky"} needs \code{lat} and \code{lon}.}

\item{interval}{what a timestamp denotes, passed to
\code{\link[=estimate_hourly_swr]{estimate_hourly_swr()}}. Default \code{"ending"}, matching the accumulated
flux convention of ERA5 and of \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}}.}

\item{lat, lon}{location in decimal degrees; taken from the \code{lat} / \code{lon}
attributes of \code{daily} or \code{donor} when \code{NULL}.}

\item{elev}{elevation, m, used only when \code{expand = TRUE}.}

\item{tz}{timezone of \code{daily} and of the output. Defaults to the \code{tz}
attribute of \code{daily}, then of \code{donor}, else \code{"Etc/GMT-12"}.}

\item{analogue_window}{half-width, in days of the year, of the donor
pool for \code{method = "fragments"} (default 15).}

\item{match_on_value}{rank candidate donor days by similarity of their
daily values, not just day-of-year (default \code{TRUE}).}

\item{sample_top_k}{sample the donor uniformly from the \code{k} best
candidates instead of always taking the closest. \code{1} (default) is
deterministic; larger values add realistic variety.}

\item{blend_hours}{smooth the borrowed shape across midnight over this
many steps either side, to avoid a sawtooth discontinuity in
temperature and humidity (default 2; \code{0} disables). Daily aggregates
are restored afterwards, so conservation is unaffected. Never applied
to rainfall or snowfall.}

\item{seed}{optional RNG seed, for reproducibility when
\code{sample_top_k > 1}.}

\item{expand}{run \code{\link[=expand_met]{expand_met()}} on the result to regenerate the
dependent variables at the new timestep.}

\item{verbose}{print progress.}
}
\value{
data frame with \code{Date} (POSIXct in \code{tz}, regular sub-daily
steps) and the same \verb{MET_*} columns as \code{daily}. Attributes \code{tz},
\code{method}, \code{timestep}, and \code{donor_days} (the analogue chosen for each
target day, \code{method = "fragments"} only).
}
\description{
Distributes daily meteorology across the day using the diurnal structure
of an hourly reanalysis record for the same location, so that a daily
series - a bias-corrected daily record, or a climate-scenario series from
a delta-change step - can drive a sub-daily lake model. Daily means (and
daily rainfall totals) are conserved exactly, with one deliberate
exception: if the wind vector components \code{MET_wnduvu} / \code{MET_wnduvv} are
present they are shaped directly and \code{MET_wndspd} is recomputed from
them, so the disaggregated speed re-aggregates to the magnitude of the
daily \emph{vector} mean rather than to a supplied daily \emph{scalar} mean speed
(the former is the smaller of the two).
}
\details{
Two ways of borrowing the structure:
\describe{
\item{\code{"fragments"}}{(default) the \emph{method of fragments}: for each
target day an analogue day is chosen from the donor record - close in
day-of-year and, when \code{match_on_value}, close in daily values - and
its within-day shape is applied to every variable at once. Because a
single donor day supplies all variables, the temperature, humidity
and wind sub-daily patterns stay mutually consistent, and day-to-day
variability in the shape is retained.}
\item{\code{"diurnal"}}{a deterministic month-by-hour mean diurnal cycle
from \code{\link[=build_diurnal_climatology]{build_diurnal_climatology()}}. Smoother and reproducible, but
every day of a given month gets the same shape.}
}

Shortwave radiation is handled separately and by default comes from
solar geometry (\code{\link[=estimate_hourly_swr]{estimate_hourly_swr()}}), which guarantees a
single-peaked curve, true night-time zeros and a peak at local solar
noon. Rainfall in \code{"fragments"} mode borrows its shape from a \emph{wet}
donor day, chosen by closeness of daily total, so that wet-hour
intermittency is preserved; a dry target day stays dry throughout.

Variables present in \code{daily} but not in \code{donor} are held constant
through each day. Wind direction (\code{MET_wnddir}) is never shaped about a
daily mean - it is recomputed from the disaggregated \code{MET_wnduvu} /
\code{MET_wnduvv} when those are present, otherwise held constant per day.
}
\examples{
set.seed(1)
## donor: two years of synthetic hourly data with a diurnal cycle
t <- seq(as.POSIXct("2022-01-01", tz = "Etc/GMT-12"), by = "hour",
         length.out = 24 * 730)
hr <- as.integer(format(t, "\%H"))
donor <- data.frame(Date = t,
                    MET_tmpair = 15 + 5 * sin((hr - 9) / 24 * 2 * pi) + rnorm(length(t)),
                    MET_wndspd = pmax(0.1, 4 + 2 * sin((hr - 12) / 24 * 2 * pi)))
## daily series to disaggregate
daily <- data.frame(Date = seq(as.Date("2024-01-01"), by = "day", length.out = 10),
                    MET_tmpair = rnorm(10, 16, 2),
                    MET_wndspd = runif(10, 2, 7))
out <- disaggregate_met_to_hourly(daily, donor, swr = "shape", verbose = FALSE)
head(out)
}
\seealso{
\code{\link[=build_diurnal_climatology]{build_diurnal_climatology()}}, \code{\link[=estimate_hourly_swr]{estimate_hourly_swr()}},
\code{\link[=met_to_daily]{met_to_daily()}} for the reverse operation.
}
