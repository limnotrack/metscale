% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_cmip6_point.R
\name{extract_cmip6_point}
\alias{extract_cmip6_point}
\title{Extract daily CMIP6 / CCAM projection meteorology for a point}
\usage{
extract_cmip6_point(
  path,
  lon,
  lat,
  vars = c("MET_tmpair", "MET_pprain", "MET_wndspd", "MET_radswd", "MET_humrel",
    "MET_radlwd", "MET_prsttn"),
  experiments = NULL,
  method = c("bilinear", "nearest"),
  calendar = c("auto", "365_day", "360_day", "366_day", "standard"),
  tz = "Etc/GMT-12",
  verbose = TRUE
)
}
\arguments{
\item{path}{directory holding the \code{.nc} files, or a character vector of
file paths.}

\item{lon, lat}{point of interest, decimal degrees (WGS84).}

\item{vars}{variables to return, as AEME \verb{MET_*} names or CMIP short names
(do not mix the two). Default is the seven mapped variables; only those
present on disk are returned.}

\item{experiments}{optional character vector to keep (e.g.
\code{c("historical", "ssp245")}); \code{NULL} (default) returns every experiment
found.}

\item{method}{\code{"bilinear"} (default) interpolation between the four
surrounding grid nodes, or \code{"nearest"} grid node.}

\item{calendar}{\code{"auto"} (default, read from the \code{time} variable's
\code{calendar} attribute), or force one of \code{"365_day"}, \code{"360_day"},
\code{"366_day"}, \code{"standard"}. A \verb{365_day} / \verb{360_day} series is mapped onto
real dates, so it has no 29 February (and, for \verb{360_day}, no 31st).}

\item{tz}{time zone recorded on the result; the series is daily and
carries no time of day. Default \code{"Etc/GMT-12"}.}

\item{verbose}{print each file as it is read.}
}
\value{
a data frame with \code{Date} (class \code{Date}), \code{experiment} (character)
and one column per requested variable - stacked over experiments, wide
over variables. Attributes \code{lon}, \code{lat}, \code{method}, \code{tz}.
}
\description{
Reads local daily climate-projection netCDF files - one variable per file,
as produced by the NIWA CCAM downscaling of CMIP6 - samples them at a
point, decodes the model calendar to real dates, and returns the series in
AEME \verb{MET_*} names and units. Built for the delta-change step of
\link{scenario_workflow}: extract the model \emph{historical} and \emph{future} series
with this function, form monthly change factors, and apply them to a
\code{\link[=bias_correct_daily_baseline]{bias_correct_daily_baseline()}}.
}
\details{
Files are matched by the convention \verb{<variable>_<experiment>_*.nc} (for
example \code{tas_ssp245_NorESM2-MM_CCAM_daily_NZ5km_bc.nc} -> variable \code{tas},
experiment \code{ssp245}). Recognised variables and their AEME targets:
\code{tas} -> \code{MET_tmpair}, \code{pr} -> \code{MET_pprain}, \code{sfcWind} -> \code{MET_wndspd},
\code{rsds} -> \code{MET_radswd}, \code{hurs} -> \code{MET_humrel}, \code{rlds} -> \code{MET_radlwd},
\code{ps} -> \code{MET_prsttn}.

Units are converted from each file's own \code{units} attribute: Kelvin ->
degC, \verb{kg m-2 s-1} -> mm day-1, metres -> mm. Everything else is passed
through (the CCAM \code{pr} files are already mm day-1). \verb{_FillValue} is applied
by \pkg{ncdf4}.
}
\examples{
\dontrun{
cmip <- extract_cmip6_point("inst/extdata/rotorua_cmip6",
                            lon = 176.2717, lat = -38.0790)
split(cmip, cmip$experiment) |> lapply(head, 2)
}
}
\seealso{
\link{scenario_workflow}, \code{\link[=bias_correct_daily_baseline]{bias_correct_daily_baseline()}},
\code{\link[=disaggregate_met_to_hourly]{disaggregate_met_to_hourly()}}
}
