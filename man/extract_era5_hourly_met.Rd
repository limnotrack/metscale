% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_era5_hourly_met.R
\name{extract_era5_hourly_met}
\alias{extract_era5_hourly_met}
\title{Extract hourly ERA5-Land meteorology for a point or lake}
\usage{
extract_era5_hourly_met(
  path,
  lon = NULL,
  lat = NULL,
  geom = NULL,
  years = NULL,
  months = 1:12,
  variables = c("2m_temperature", "2m_dewpoint_temperature", "10m_u_component_of_wind",
    "10m_v_component_of_wind", "surface_solar_radiation_downwards",
    "surface_thermal_radiation_downwards", "total_precipitation", "snowfall",
    "surface_pressure"),
  method = c("bilinear", "nearest", "area", "area_mean"),
  tz = "Etc/GMT-12",
  format = c("AEME", "LER", "raw"),
  precip_units = c("mm/hr", "m/day", "mm/day", "m/hr"),
  pressure_units = c("Pa", "hPa"),
  file_template = "nz_era5-land_\%d_\%02d_\%s_daily.nc",
  area_crs = 2193,
  outfile = NULL,
  fill_gaps = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{path}{directory holding the ERA5-Land netCDF files.}

\item{lon, lat}{point of interest, decimal degrees (WGS84). Ignored if
\code{geom} is supplied.}

\item{geom}{an \code{sf}/\code{sfc} point or polygon (any CRS) identifying the
location; overrides \code{lon}/\code{lat}.}

\item{years}{integer vector of years to read. \code{NULL} (default) uses
every year found on disk for the requested variables.}

\item{months}{integer vector of months to read (default \code{1:12}).}

\item{variables}{ERA5-Land variable names to read. Defaults to the nine
standard meteorological forcing variables.}

\item{method}{\code{"nearest"}, \code{"bilinear"} (default), \code{"area"} or
\code{"area_mean"} - see Details.}

\item{tz}{output time zone. Default \code{"Etc/GMT-12"} = fixed NZST
(UTC+12, no daylight saving) which keeps a gap-free regular hourly
series. Use \code{"Pacific/Auckland"} for civil NZ time (a duplicated hour
every April, a missing hour every September).}

\item{format}{\code{"AEME"} (default, \verb{MET_*} names), \code{"LER"} (LakeEnsemblR
names) or \code{"raw"} (ERA5 short names).}

\item{precip_units}{\code{"mm/hr"} (default), \code{"m/day"}, \code{"mm/day"} or
\code{"m/hr"} - applied to precipitation and snowfall.}

\item{pressure_units}{\code{"Pa"} (default) or \code{"hPa"}.}

\item{file_template}{\code{sprintf()} template for the file names, taking the
year (integer), month (integer) and ERA5 variable name in that order.
Default \code{"nz_era5-land_\%d_\%02d_\%s_daily.nc"}.}

\item{area_crs}{projected CRS (EPSG code or WKT) used to compute polygon
intersection areas for \code{method = "area"}. Default \code{2193} (NZTM 2000);
use a projection appropriate to your region.}

\item{outfile}{optional path; if given the frame is written there with
\code{utils::write.csv(..., row.names = FALSE)}, time formatted
\code{"\%Y-\%m-\%d \%H:\%M:\%S"}.}

\item{fill_gaps}{reindex onto a complete regular hourly sequence
(missing steps become \code{NA}). Default \code{TRUE}.}

\item{verbose}{print progress messages.}
}
\value{
a data frame: a time column (\code{Date} for AEME, else \code{datetime};
POSIXct in \code{tz}) plus one column per variable in the chosen naming
\code{format}. Derived \verb{*_wndspd}, \verb{*_wnddir} and \verb{*_humrel} columns are
added when their inputs are present. Attributes \code{lon}, \code{lat},
\code{method}, \code{tz}, \code{n_cells} describe the extraction.
}
\description{
Reads monthly hourly ERA5-Land netCDF files from \code{path}, pulls a time
series for a location, de-accumulates the flux variables, converts
everything to standard lake-model units (\code{\link[=met_vars]{met_vars()}}), shifts the time
stamps from UTC to \code{tz} and returns a tidy wide data frame ready to be
written to CSV.
}
\details{
Files are located with \code{file_template}, which defaults to the naming the
LERNZmp download scripts produce,
\verb{nz_era5-land_<YYYY>_<MM>_<variable>_daily.nc} - note the data are hourly
despite the \verb{_daily} suffix.

The location can be given as
\itemize{
\item \code{lon} / \code{lat} scalars, or
\item \code{geom} - an \code{sf}/\code{sfc} point or polygon in any CRS.
}

Spatial sampling (\code{method}):
\itemize{
\item \code{"nearest"}  - value of the single closest grid node to the point
/ polygon centroid.
\item \code{"bilinear"} - bilinear interpolation between the four grid nodes
surrounding the point / centroid.
\item \code{"area"}     - area-weighted mean of every ERA5 grid cell whose
footprint overlaps the polygon (use when a lake
straddles several cells). Weights are intersection
areas computed in the \code{area_crs} projection.
\item \code{"area_mean"}- unweighted mean of the overlapping cells.
\code{"area"} / \code{"area_mean"} require a polygon \code{geom}; they fall back to
\code{"bilinear"} for a point or a lake smaller than one grid cell.
}

Unit handling:
\itemize{
\item 2m_temperature / 2m_dewpoint_temperature   K            -> degC
\item 10m_u/v_component_of_wind                  m s-1        -> m s-1 (unchanged)
\item surface_pressure                           Pa           -> Pa   (or hPa)
\item surface_solar_radiation_downwards (ssrd)   J m-2 accum. -> W m-2
\item surface_thermal_radiation_downwards (strd) J m-2 accum. -> W m-2
\item total_precipitation (tp)                   m accum.     -> mm hr-1 (or m day-1)
\item snowfall (sf)                              m accum.     -> mm hr-1 (or m day-1)
}

ERA5-Land accumulations run from 00:00 UTC and reset at 01:00 UTC each
day, so the hourly amount is recovered as a first difference except at
the 01:00 UTC step (and the first record), where the stored value is
already the hourly amount.
}
\examples{
\dontrun{
## by coordinate
met <- extract_era5_hourly_met(
  path = "download_era5-land/era5_netcdf",
  lon  = 176.2717, lat = -38.0790, years = 2023:2024)

## by lake polygon, area-weighted over every overlapping grid cell
lakes <- readRDS("gis/lake_shapefile/lernzmp_lakes_master.rds")$updated
poly  <- lakes[lakes$name_final == "Rotorua", ]
met   <- extract_era5_hourly_met(
  path = "download_era5-land/era5_netcdf", geom = poly,
  method = "area", years = 2023:2024,
  outfile = "rotorua_era5_hourly_met.csv")
}
}
