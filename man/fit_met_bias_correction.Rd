% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_met_bias_correction.R
\name{fit_met_bias_correction}
\alias{fit_met_bias_correction}
\title{Fit a bias correction for ERA5-Land meteorology from local observations}
\usage{
fit_met_bias_correction(
  era5,
  obs,
  vars = NULL,
  method = "scale",
  transform = "auto",
  by = c("doy-loess", "month", "none"),
  cv = c("loyo", "none"),
  min_n = 60,
  cv_min_months = 8,
  loess_span = 0.25,
  robust = FALSE,
  nquantiles = 50,
  swr_daytime_wm2 = 5,
  verbose = TRUE
)
}
\arguments{
\item{era5}{data frame with \code{Date} plus \verb{MET_*} columns (extractor output).}

\item{obs}{data frame with \code{Date} plus \verb{MET_*} columns
(\code{\link[=prepare_obs_met]{prepare_obs_met()}} output). Must be the same resolution and time
zone as \code{era5}.}

\item{vars}{character vector of \verb{MET_*} columns to correct. \code{NULL}
(default) uses the columns common to both, excluding derived/direction
fields (\code{MET_wnddir}, \code{MET_wnduvu}, \code{MET_wnduvv}, \code{MET_cldcvr},
\code{MET_prvapr}, \code{MET_prmslp}).}

\item{method}{\code{"scale"} (default), \code{"variance"}, \code{"linear"}, \code{"eqm"} or
\code{"qdm"}; or a named vector, e.g.
\code{c(MET_wndspd = "eqm", MET_tmpair = "scale")}.}

\item{transform}{\code{"auto"} (ratio for \code{MET_wndspd}/\code{MET_radswd}/
\code{MET_pprain}/\code{MET_ppsnow}, additive otherwise), \code{"additive"} or
\code{"ratio"}; or a named vector. Only used by \code{method = "scale"}.}

\item{by}{\code{"doy-loess"} (default), \code{"month"} or \code{"none"}. Coerced to
\code{"month"} for methods other than \code{"scale"}.}

\item{cv}{\code{"loyo"} (leave-one-year-out, default) or \code{"none"} - controls
whether the skill table includes out-of-sample rows.}

\item{min_n}{minimum paired records per monthly stratum (default 60).}

\item{cv_min_months}{a year is usable as a leave-one-out fold only if it
has at least this many months of overlap (default 8).}

\item{loess_span}{span for the day-of-year loess (default 0.25).}

\item{robust}{use \code{MASS::rlm} for \code{method = "linear"} (default \code{FALSE}).}

\item{nquantiles}{number of quantiles for \code{"eqm"}/\code{"qdm"} (default 50).}

\item{swr_daytime_wm2}{ERA5 shortwave threshold (W/m2) above which a
record counts as daytime for the shortwave fit (default 5).}

\item{verbose}{print progress and the skill table.}
}
\value{
an object of class \code{met_biascorr}: a list with \code{models} (per
variable), \code{skill} (data frame: \code{variable}, \code{stage} in
\code{raw}/\code{corrected}/\code{cv_raw}/\code{cv_corrected}, \code{n}, \code{bias}, \code{mae},
\code{rmse}, \code{r}, \code{kge}), \code{training} (window, resolution, tz, years,
station) and \code{call}.
}
\description{
Joins an ERA5 met frame (from \code{\link[=extract_era5_hourly_met]{extract_era5_hourly_met()}} /
\code{\link[=extract_era5_lake_met]{extract_era5_lake_met()}}) to a matching frame of locally measured
meteorology (from \code{\link[=prepare_obs_met]{prepare_obs_met()}}) over their common period and
fits, per variable, a transfer function that maps ERA5 onto the
observations. The fitted object is applied to the full ERA5 record with
\code{\link[=apply_met_bias_correction]{apply_met_bias_correction()}}.
}
\details{
Methods (set globally via \code{method}, or per variable with a named
vector):
\describe{
\item{\code{"scale"}}{(default) a per-month offset - additive
(\code{obs - era5}) or multiplicative (\code{sum(obs) / sum(era5)}), chosen by
\code{transform}. With \code{by = "doy-loess"} the 12 monthly offsets are
loess-smoothed across the day-of-year (as
\code{nz_climate_projections/climate_shift.R}).}
\item{\code{"variance"}}{per-month mean and standard-deviation matching:
\code{mean_o + (x - mean_e) * sd_o / sd_e}.}
\item{\code{"linear"}}{per-month least squares \code{obs ~ a + b * era5}
(\code{robust = TRUE} uses \code{MASS::rlm}).}
\item{\code{"eqm"}}{empirical quantile mapping per month - remap the ERA5
CDF onto the observed CDF, linear interpolation, constant-tail
extrapolation.}
\item{\code{"qdm"}}{quantile delta mapping - as \code{"eqm"} but preserves the
ERA5 anomaly relative to its training quantile, so trends/among-year
signal survive extrapolation.}
}
Any stratum with fewer than \code{min_n} pairs falls back to the pooled
(all-month) fit. Shortwave is fitted on daytime records only.
}
\seealso{
\code{\link[=prepare_obs_met]{prepare_obs_met()}}, \code{\link[=apply_met_bias_correction]{apply_met_bias_correction()}}
}
