utils::globalVariables(c(
  # NSE column names used by the ERA5 acquisition helpers
  "DateTime",
  "aeme",
  "era5",
  "era5_ref_table",
  # plot_extract_grid(): ggplot2 aes() column
  "weight",
  # plot_era5(): ggplot2 aes() columns
  "x", "y", "value"
))
